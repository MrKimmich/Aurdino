int VPin = A2;
int lightVal;
int DELAY = 500;

void setup() {
  pinMode(VPin, INPUT);
  Serial.begin(9600);
}

void loop() {
  lightVal = analogRead(VPin);
  Serial.println(lightVal);
  delay(DELAY);
}
  
